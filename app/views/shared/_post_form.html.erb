<%= form_for @post do |f| %>
<%= render 'shared/error_messages', :object => f.object %> 
<div class="field">
	<%= f.text_area :content %> 
	<p><span id="counter"></span> char left</p>	
</div>

<div class="actions"> 
	<%= f.submit "Submit" %>
</div> 
<% end %>